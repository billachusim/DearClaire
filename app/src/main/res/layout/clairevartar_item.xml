<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="clairevartar"
            type="com.mobymagic.clairediary.vo.ClaireVartar" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/claire_vartar_image"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_margin="4dp"
            android:contentDescription="@string/select_your_avartar"
            android:scaleType="fitCenter"
            app:imageUrl="@{clairevartar.imageUrl}"
            tools:src="@drawable/dummy_claire_vartar" />

    </LinearLayout>
</layout>